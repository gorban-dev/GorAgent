# 🤖 Development Assistant - Руководство

> Ассистент для разработки проекта с доступом к документации и git-репозиторию

---

## 📋 Содержание

1. [Введение](#введение)
2. [Быстрый старт](#быстрый-старт)
3. [Архитектура](#архитектура)
4. [Возможности](#возможности)
5. [API Endpoints](#api-endpoints)
6. [Примеры использования](#примеры-использования)
7. [Команды](#команды)
8. [Конфигурация](#конфигурация)

---

## 🎯 Введение

**Development Assistant** — это интеллектуальный ассистент для разработки, который комбинирует:

- **RAG (Retrieval-Augmented Generation)** — поиск в индексированной документации проекта
- **MCP Git Server** — доступ к информации о git-репозитории
- **OpenAI GPT** — генерация ответов и помощь в разработке

### Что он умеет?

✅ Искать информацию в документации проекта (README, API docs, код)  
✅ Находить определения функций, классов и методов  
✅ Показывать git-статус, ветки, коммиты  
✅ Отвечать на вопросы о best practices  
✅ Подсказывать фрагменты кода и примеры использования  
✅ Понимать контекст проекта и текущих изменений  

---

## 🚀 Быстрый старт

### Шаг 1: Индексация проекта

Перед первым использованием нужно проиндексировать документацию и код проекта:

```bash
# Автоматическая индексация всех файлов проекта
node index-project-docs.js
```

Это создаст файл `project-index.json` с эмбеддингами для семантического поиска.

**Что индексируется:**
- 📄 Markdown файлы (`.md`)
- 📝 Текстовые файлы (`.txt`)
- 💻 Исходный код (`.js`, `.ts`, `.py`, `.java`, `.go`, `.rs`)

**Игнорируются:**
- `node_modules/`
- `.git/`
- `dist/`, `build/`
- Скрытые директории (начинаются с `.`)

### Шаг 2: Запуск сервера

```bash
npm start
```

### Шаг 3: Открыть веб-интерфейс

Откройте в браузере:

```
http://localhost:3000/dev-assistant
```

### Шаг 4: Начните задавать вопросы!

Примеры вопросов:
- "Как запустить проект?"
- "Где определена функция processDocument?"
- "Какая текущая ветка?"
- "Что изменилось в проекте?"
- "Покажи примеры использования MCP"

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    Development Assistant                     │
│                    (dev-assistant.js)                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ├─────────────────────────────────┐
                              │                                 │
                              ▼                                 ▼
┌──────────────────────────────────────┐   ┌────────────────────────────┐
│        Document Indexer (RAG)        │   │      MCP Git Server        │
│      (document-indexer.js)           │   │   (mcp-server-git.js)      │
├──────────────────────────────────────┤   ├────────────────────────────┤
│ • Индексация файлов                  │   │ • git_current_branch       │
│ • Генерация эмбеддингов (OpenAI)     │   │ • git_status               │
│ • Чанкинг документов                 │   │ • git_recent_commits       │
│ • Семантический поиск                │   │ • git_branches             │
│ • Косинусное сходство                │   │ • git_diff                 │
└──────────────────────────────────────┘   │ • git_file_history         │
                                            │ • git_blame                │
                                            │ • git_contributors         │
                                            └────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       OpenAI GPT API                        │
│                  (генерация ответов)                        │
└─────────────────────────────────────────────────────────────┘
```

### Компоненты

#### 1. **Document Indexer** (`document-indexer.js`)

Система индексации документов с эмбеддингами:
- Разбивка текста на чанки (500 токенов с перекрытием 50)
- Генерация эмбеддингов через OpenAI API (`text-embedding-3-small`)
- Сохранение индекса в JSON
- Семантический поиск по косинусному сходству

#### 2. **MCP Git Server** (`mcp-server-git.js`)

MCP сервер для работы с git:
- Выполнение git команд через `child_process`
- Парсинг вывода git
- Структурированный ответ в JSON формате

#### 3. **Project Doc Indexer** (`index-project-docs.js`)

Автоматическая индексация проекта:
- Рекурсивный обход директорий
- Извлечение структуры кода (функции, классы)
- Извлечение заголовков из Markdown
- Метаданные для каждого файла

#### 4. **Dev Assistant** (`dev-assistant.js`)

Главный компонент:
- Intent detection (определение намерения пользователя)
- Сбор контекста (git + документация)
- Генерация промптов для GPT
- Форматирование ответов

---

## 🎨 Возможности

### 1. 📚 Поиск в документации

Семантический поиск по всей документации проекта:

```
Вопрос: "Как работает система напоминаний?"

Ответ: [находит релевантные части README и кода]
```

### 2. 🔍 Поиск в коде

Находит определения функций, классов, методов:

```
Вопрос: "Где определена функция processDocument?"

Ответ: [показывает файл, строки и код функции]
```

### 3. 🔀 Git информация

Доступ к информации о репозитории:

```
Вопрос: "Какая текущая ветка?"

Ответ: "Текущая ветка: main"
```

```
Вопрос: "Что изменилось?"

Ответ: 
"Изменено:
- server.js
- dev-assistant.js
Добавлено:
- mcp-server-git.js"
```

### 4. 💡 Best Practices

Советы и рекомендации:

```
Вопрос: "Best practices для MCP серверов"

Ответ: [находит в документации раздел о best practices]
```

### 5. 🎯 Контекстное понимание

Ассистент понимает контекст проекта:
- Текущую ветку
- Несохраненные изменения
- Последние коммиты
- Структуру проекта

---

## 🌐 API Endpoints

### POST `/api/dev-assistant/chat`

Основной endpoint для общения с ассистентом.

**Request:**
```json
{
  "message": "Как запустить проект?"
}
```

**Response:**
```json
{
  "success": true,
  "answer": "Для запуска проекта выполните команду `npm start`...",
  "intent": "docs",
  "context": {
    "git": {
      "branch": "main",
      "hasChanges": false
    },
    "docsCount": 3
  },
  "usage": {
    "prompt_tokens": 1234,
    "completion_tokens": 567,
    "total_tokens": 1801
  }
}
```

### GET `/api/dev-assistant/status`

Получить статус системы и git-репозитория.

**Response:**
```json
{
  "success": true,
  "indexStats": {
    "totalDocuments": 25,
    "totalChunks": 342,
    "model": "text-embedding-3-small"
  },
  "gitToolsCount": 8,
  "git": {
    "branch": "main",
    "hasChanges": true,
    "status": {
      "modified": ["server.js"],
      "added": [],
      "deleted": [],
      "untracked": []
    },
    "recentCommits": [...]
  }
}
```

### POST `/api/dev-assistant/index`

Запустить индексацию проекта.

**Response:**
```json
{
  "success": true,
  "files": 25,
  "path": "/path/to/project-index.json",
  "size": 1234567,
  "documents": 25,
  "chunks": 342
}
```

### POST `/api/dev-assistant/git/:tool`

Выполнить git команду напрямую.

**Доступные tools:**
- `git_current_branch`
- `git_status`
- `git_recent_commits`
- `git_branches`
- `git_diff`
- `git_file_history`
- `git_blame`
- `git_contributors`

**Example:**
```bash
curl -X POST http://localhost:3000/api/dev-assistant/git/git_recent_commits \
  -H "Content-Type: application/json" \
  -d '{"limit": 5}'
```

**Response:**
```json
{
  "success": true,
  "commits": [
    {
      "hash": "abc1234",
      "author": "John Doe",
      "date": "2024-12-19",
      "message": "Add dev assistant"
    }
  ],
  "count": 5
}
```

---

## 💻 Примеры использования

### Пример 1: Поиск документации

```javascript
const response = await fetch('http://localhost:3000/api/dev-assistant/chat', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    message: 'Как использовать Document Indexer?'
  })
});

const data = await response.json();
console.log(data.answer);
```

### Пример 2: Git статус

```javascript
const response = await fetch('http://localhost:3000/api/dev-assistant/git/git_status', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' }
});

const data = await response.json();
console.log('Изменено файлов:', data.files.modified.length);
```

### Пример 3: Программная индексация

```javascript
const ProjectDocIndexer = require('./index-project-docs');

const indexer = new ProjectDocIndexer('./my-project');
const result = await indexer.indexProject();

console.log('Проиндексировано файлов:', result.files);
```

---

## 🎮 Команды

### `/help`

Показывает полную справку по возможностям ассистента.

```
Вы: /help

Ассистент: [показывает полное руководство с примерами]
```

### `/status`

Показывает текущий статус проекта и git-репозитория.

```
Вы: /status

Ассистент:
# 📊 Статус проекта

## Git репозиторий
- **Ветка:** main
- **Изменений:** Чистая рабочая директория
- **Последний коммит:** Add dev assistant

## Документация
- **Документов:** 25
- **Чанков:** 342
- **Модель эмбеддингов:** text-embedding-3-small
```

---

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# OpenAI API ключ (обязательно)
OPENAI_API_KEY=sk-...

# Модель OpenAI (опционально, по умолчанию gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Порт сервера (опционально, по умолчанию 3000)
PORT=3000
```

### Настройки индексации

В `index-project-docs.js`:

```javascript
// Расширения файлов для индексации
this.codeExtensions = ['.js', '.ts', '.jsx', '.tsx', '.py', '.java', '.go', '.rs'];
this.docExtensions = ['.md', '.txt', '.rst'];

// Игнорируемые директории
this.ignoreDirs = ['node_modules', '.git', 'dist', 'build', '.next', 'coverage', 'logs'];
```

### Настройки чанкинга

В `document-indexer.js`:

```javascript
this.chunkSize = 500; // размер чанка в токенах
this.chunkOverlap = 50; // перекрытие между чанками
```

### Настройки Dev Assistant

В `dev-assistant.js`:

```javascript
this.systemPrompt = `Вы — ассистент для разработки проекта...`;
```

---

## 🔧 Расширение функционала

### Добавление новых git команд

В `mcp-server-git.js`:

```javascript
// Добавьте новый инструмент
{
    name: 'git_my_command',
    description: 'Описание команды',
    inputSchema: {
        type: 'object',
        properties: {
            param: {
                type: 'string',
                description: 'Параметр'
            }
        },
        required: ['param']
    }
}

// Добавьте обработчик
async myCommand(param) {
    const result = await this.execGit(`my-command ${param}`);
    return {
        success: true,
        result
    };
}
```

### Добавление новых типов файлов

В `index-project-docs.js`:

```javascript
// Добавьте расширение
this.codeExtensions.push('.cpp', '.c', '.h');

// Добавьте язык
getLanguage(filepath) {
    const ext = path.extname(filepath);
    const langMap = {
        // ...существующие
        '.cpp': 'cpp',
        '.c': 'c',
        '.h': 'c'
    };
    return langMap[ext] || 'unknown';
}
```

### Кастомизация Intent Detection

В `dev-assistant.js`:

```javascript
detectIntent(message) {
    const lowerMessage = message.toLowerCase();
    
    // Добавьте новый intent
    if (lowerMessage.match(/\b(тест|test|тестирование)\b/)) {
        return 'testing';
    }
    
    // ...остальные intents
}
```

---

## 📊 Метрики и статистика

### Стоимость индексации

**OpenAI Embeddings API** (`text-embedding-3-small`):
- Цена: **$0.02 / 1M токенов**

**Примеры:**
- Небольшой проект (10 файлов, ~50KB): **~$0.001** (~0.1 цента)
- Средний проект (50 файлов, ~500KB): **~$0.01** (~1 цент)
- Большой проект (200 файлов, ~2MB): **~$0.04** (~4 цента)

### Стоимость запросов

**OpenAI Chat API** (`gpt-4o-mini`):
- Входящие: **$0.15 / 1M токенов**
- Исходящие: **$0.60 / 1M токенов**

**Типичный запрос:**
- Контекст: ~1000 токенов
- Ответ: ~500 токенов
- Стоимость: **~$0.0005** (~0.05 цента)

---

## 🐛 Отладка и решение проблем

### Проблема: Индекс не загружается

**Решение:**
```bash
# Проверьте существование индекса
ls -la project-index.json

# Если нет, создайте
node index-project-docs.js
```

### Проблема: Git команды не работают

**Решение:**
```bash
# Убедитесь что в директории есть git репозиторий
git status

# Проверьте права доступа
ls -la .git
```

### Проблема: Поиск не находит документы

**Причины:**
1. Индекс устарел — переиндексируйте проект
2. Запрос слишком специфичный — попробуйте более общий
3. Порог релевантности слишком высокий

**Решение:**
```bash
# Переиндексация
node index-project-docs.js

# Проверка статистики
curl http://localhost:3000/api/dev-assistant/status
```

### Проблема: Медленные ответы

**Оптимизация:**
1. Уменьшите `topK` (количество источников)
2. Используйте более быструю модель (gpt-4o-mini)
3. Кэшируйте частые запросы

---

## 🚀 Best Practices

### 1. Регулярная переиндексация

Запускайте индексацию после значительных изменений:

```bash
# В git hook
cat > .git/hooks/post-merge << 'EOF'
#!/bin/bash
node index-project-docs.js
EOF
chmod +x .git/hooks/post-merge
```

### 2. Использование .gitignore

Добавьте индекс в `.gitignore`:

```gitignore
# Dev Assistant
project-index.json
```

### 3. CI/CD интеграция

Автоматическая индексация в CI:

```yaml
# .github/workflows/index-docs.yml
name: Index Documentation
on:
  push:
    branches: [main]

jobs:
  index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: node index-project-docs.js
      - uses: actions/upload-artifact@v2
        with:
          name: project-index
          path: project-index.json
```

### 4. Оптимизация промптов

Делайте запросы максимально конкретными:

❌ "Расскажи про систему"  
✅ "Как работает система напоминаний в этом проекте?"

❌ "git"  
✅ "Покажи последние 5 коммитов в текущей ветке"

### 5. Мониторинг использования API

Отслеживайте токены:

```javascript
// В коде приложения
let totalTokens = 0;

response.usage.forEach(usage => {
  totalTokens += usage.total_tokens;
});

console.log('Использовано токенов:', totalTokens);
console.log('Примерная стоимость:', (totalTokens * 0.0000015).toFixed(4), 'USD');
```

---

## 📚 Дополнительные ресурсы

### Связанная документация

- [Document Indexer Guide](./DOCUMENT-INDEXER-GUIDE.md)
- [MCP Multi Guide](./MCP-MULTI-GUIDE.md)
- [Quick Start](./QUICK-START.md)

### Примеры использования

- Веб-интерфейс: `http://localhost:3000/dev-assistant`
- RAG Chat: `http://localhost:3000/rag-chat.html`
- MCP Demo: `http://localhost:3000/mcp-demo`

### Файлы проекта

- `dev-assistant.js` - Основной класс ассистента
- `dev-assistant.html` - Веб-интерфейс
- `mcp-server-git.js` - MCP сервер для git
- `index-project-docs.js` - Скрипт индексации
- `document-indexer.js` - Система индексации

---

## 🤝 Вклад в проект

Хотите улучшить Dev Assistant? Вот несколько идей:

### Идеи для улучшений

1. **Поддержка больше языков программирования**
   - Rust, C++, Java, Kotlin, Swift
   - Специфичный парсинг синтаксиса

2. **Расширенный git функционал**
   - git blame с аннотациями
   - Визуализация истории веток
   - Интеграция с GitHub/GitLab API

3. **Улучшенный intent detection**
   - Машинное обучение для определения намерений
   - Multi-intent queries
   - Контекстная история диалога

4. **Кэширование**
   - Redis для кэша эмбеддингов
   - LRU кэш для частых запросов
   - Incremental indexing

5. **Интеграция с IDE**
   - VS Code extension
   - JetBrains plugin
   - Vim/Neovim plugin

---

## 📄 Лицензия

MIT License - свободно используйте в своих проектах!

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте [FAQ](#отладка-и-решение-проблем)
2. Посмотрите [примеры использования](#примеры-использования)
3. Изучите исходный код

---

**Приятного использования! 🚀**
