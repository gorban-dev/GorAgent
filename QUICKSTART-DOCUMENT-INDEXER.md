# 📚 Document Indexer — Быстрый старт

> **Система индексации документов с эмбеддингами и семантическим поиском**

## 🚀 Запуск за 3 минуты

### Шаг 1: Убедитесь что сервер запущен

```bash
npm start
```

Сервер должен быть запущен на `http://localhost:3000`

### Шаг 2: Откройте интерфейс

Откройте в браузере:
```
http://localhost:3000/document-index-demo
```

### Шаг 3: Загрузите примеры

Нажмите кнопку **"📋 Загрузить примеры"** — это автоматически:
- Загрузит 2 примера документов (ML Introduction, Embeddings Guide)
- Разобьет их на чанки
- Создаст эмбеддинги через OpenAI API
- Добавит в индекс

### Шаг 4: Попробуйте поиск

В разделе "🔍 Семантический поиск":
1. Введите запрос, например: `"машинное обучение"`
2. Нажмите **"🔍 Искать"**
3. Получите релевантные результаты с оценкой сходства

### Шаг 5: Сохраните индекс

Нажмите **"💾 Сохранить индекс"** — индекс сохранится в файл `document-index.json`

## 📊 Что вы увидите?

### Интерфейс

```
┌────────────────────────────────────────────────────────────┐
│  📄 Добавить документы         │  📊 Статистика индекса    │
│  ├─ Текст документа            │  ├─ Документов: 2         │
│  ├─ Название                   │  ├─ Чанков: 14            │
│  ├─ Загрузка файлов           │  ├─ Токенов: 2500         │
│  └─ Кнопки действий           │  └─ Размер: 84 KB         │
│                                 │                           │
│  📝 Лог процесса               │  🔍 Семантический поиск   │
│  ├─ [10:30] Обработка...       │  ├─ Поисковый запрос      │
│  ├─ [10:30] Чанкинг...         │  ├─ Количество результатов│
│  └─ [10:30] ✅ Готово          │  └─ Результаты поиска     │
└────────────────────────────────────────────────────────────┘
│  📑 Индексированные документы                              │
│  ├─ ML-Introduction.md (8 чанков, 1200 токенов)           │
│  └─ Embeddings-Guide.md (6 чанков, 1300 токенов)          │
└────────────────────────────────────────────────────────────┘
```

### Процесс обработки

```
[10:30:15] Начинаем обработку документа: ML-Introduction.md
[10:30:15] Разбивка на чанки...
[10:30:15] Создано 8 чанков
[10:30:15] Генерация эмбеддингов...
[10:30:16] Обработка чанка 1/8...
[10:30:16] Обработка чанка 2/8...
...
[10:30:18] ✅ Документ обработан: 8 чанков, 1200 токенов
```

## 💡 Примеры использования

### 1. Добавление своего документа

```
1. Вставьте текст в поле "Текст документа"
2. Укажите название, например "my-document.md"
3. Нажмите "🚀 Обработать документ"
4. Дождитесь завершения (прогресс-бар покажет статус)
```

### 2. Загрузка файлов

```
1. Нажмите "Выберите файлы" в секции "или загрузите файл"
2. Выберите .txt, .md или .json файлы
3. Файл автоматически загрузится в поле "Текст документа"
4. Нажмите "🚀 Обработать документ"
```

### 3. Настройка параметров чанкинга

```
Размер чанка: 500 токенов
├─ Меньше (200-300) = более точный поиск, больше чанков
└─ Больше (800-1000) = больше контекста, меньше чанков

Перекрытие: 50 токенов
├─ Меньше (0-20) = меньше дублирования
└─ Больше (100-200) = лучше связность между чанками
```

### 4. Семантический поиск

```javascript
Примеры запросов:

• "как работает машинное обучение"
  → Найдет чанки про ML, supervised learning, neural networks

• "что такое эмбеддинги"
  → Найдет чанки про векторное представление, word2vec, BERT

• "применение AI"
  → Найдет чанки про использование ML в различных областях
```

## 📋 Основные кнопки

| Кнопка | Описание |
|--------|----------|
| 🚀 Обработать документ | Чанкинг + генерация эмбеддингов |
| 📋 Загрузить примеры | Загрузка 2 примеров документов |
| 💾 Сохранить индекс | Сохранение в document-index.json |
| 📂 Загрузить индекс | Загрузка из document-index.json |
| ⬇️ Скачать JSON | Скачать индекс как JSON файл |
| 🗑️ Очистить | Удалить все документы из индекса |
| 🔍 Искать | Семантический поиск по индексу |

## 🔧 Настройка параметров

### Размер чанка (Chunk Size)

Определяет максимальный размер одного чанка в токенах.

**Рекомендации:**
- **200-300**: Для коротких точных запросов
- **400-600**: Универсальный вариант (рекомендуется)
- **800-1000**: Для сохранения большого контекста

### Перекрытие (Chunk Overlap)

Количество токенов, дублирующихся между соседними чанками.

**Рекомендации:**
- **0**: Без дублирования (не рекомендуется)
- **20-50**: Оптимальный баланс (рекомендуется)
- **100-200**: Максимальная связность

## 🎯 API примеры

### Обработка документа

```bash
curl -X POST http://localhost:3000/api/document-indexer/process \
  -H "Content-Type: application/json" \
  -d '{
    "content": "Ваш текст документа...",
    "metadata": {
      "name": "document.md",
      "type": "markdown"
    },
    "options": {
      "chunkSize": 500,
      "chunkOverlap": 50
    }
  }'
```

### Семантический поиск

```bash
curl -X POST http://localhost:3000/api/document-indexer/search \
  -H "Content-Type: application/json" \
  -d '{
    "query": "машинное обучение",
    "topK": 5
  }'
```

### Получение статистики

```bash
curl http://localhost:3000/api/document-indexer/stats
```

## 💰 Стоимость

### OpenAI Embeddings API

Модель: `text-embedding-3-small`
- Цена: **$0.02 / 1M токенов**
- Размерность: 1536

### Примеры

| Документ | Токены | Стоимость |
|----------|--------|-----------|
| README (5k слов) | ~7,000 | $0.00014 (~0.01¢) |
| Документация (50k слов) | ~70,000 | $0.0014 (~0.14¢) |
| База знаний (500k слов) | ~700,000 | $0.014 (~1.4¢) |

**Семантический поиск:**
- Каждый запрос: ~10-50 токенов
- Стоимость: ~$0.000001 (0.0001¢)

## ❓ Частые вопросы

### Где хранится индекс?

Индекс хранится в памяти во время работы сервера. Для постоянного хранения:
- Нажмите "💾 Сохранить индекс" → сохранится в `document-index.json`
- Нажмите "📂 Загрузить индекс" → загрузится из `document-index.json`

### Какие файлы поддерживаются?

Напрямую: `.txt`, `.md`, `.json`

Для PDF нужна дополнительная обработка (см. основное руководство).

### Как работает семантический поиск?

1. Генерируется эмбеддинг для вашего запроса
2. Вычисляется косинусное сходство с эмбеддингами всех чанков
3. Возвращаются топ-K наиболее похожих чанков

### Нужно ли каждый раз генерировать эмбеддинги?

Нет! Эмбеддинги генерируются один раз при обработке документа и сохраняются в индексе. Поиск работает без дополнительных API запросов (кроме генерации эмбеддинга для самого запроса).

### Как использовать для RAG?

```javascript
// 1. Найти релевантные чанки
const search = await fetch('/api/document-indexer/search', {
  method: 'POST',
  body: JSON.stringify({ query: 'ваш вопрос', topK: 3 })
});
const { results } = await search.json();

// 2. Собрать контекст
const context = results.map(r => r.chunk.text).join('\n\n');

// 3. Отправить в LLM
const chat = await fetch('/api/chat', {
  method: 'POST',
  body: JSON.stringify({
    message: `Контекст:\n${context}\n\nВопрос: ваш вопрос`,
    systemPrompt: 'Отвечай на основе контекста.'
  })
});
```

## 🔗 Полезные ссылки

- 📖 [Полное руководство](./DOCUMENT-INDEXER-GUIDE.md)
- 🏠 [Главная страница проекта](./README.md)
- 🔧 [OpenAI Embeddings API](https://platform.openai.com/docs/guides/embeddings)

## 🎉 Готово!

Теперь вы готовы использовать Document Indexer для:
- ✅ Индексации вашей документации
- ✅ Семантического поиска по документам
- ✅ Построения RAG приложений
- ✅ Создания локальной базы знаний

**Следующие шаги:**
1. Добавьте свои документы
2. Настройте параметры чанкинга под свои задачи
3. Используйте API для интеграции в ваши приложения

---

**💡 Совет:** Начните с примеров документов, чтобы понять как работает система, затем добавляйте свои документы.

**🆘 Нужна помощь?** Откройте [полное руководство](./DOCUMENT-INDEXER-GUIDE.md) для детальной информации.


