# 📱 Руководство по Android Emulator MCP Server

## Описание

Android Emulator MCP Server — это специализированный MCP сервер для управления Android эмуляторами через ADB команды. Он позволяет запускать, останавливать эмуляторы, выполнять команды и многое другое.

## ✨ Возможности

- 📋 **Список эмуляторов** — получение всех доступных AVD
- ▶️ **Запуск эмулятора** — автоматический запуск и ожидание загрузки
- ⏹️ **Остановка эмулятора** — безопасное завершение работы
- 📊 **Проверка статуса** — информация о запущенных устройствах
- 📦 **Установка APK** — установка приложений на эмулятор
- ⌨️ **ADB команды** — выполнение произвольных adb команд
- 📸 **Скриншоты** — создание снимков экрана

## 🚀 Быстрый старт

### 1. Установка зависимостей

Убедитесь, что у вас установлен Android SDK и ADB доступен в PATH:

```bash
# Проверка ADB
adb version

# Если ADB не найден, добавьте в PATH
export ANDROID_HOME=/path/to/android/sdk
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$ANDROID_HOME/emulator
```

### 2. Запуск сервера

**Вариант 1: Запуск только Android сервера**
```bash
node mcp-server-android.js
```

**Вариант 2: Запуск всех MCP серверов**
```bash
bash start-mcp-servers.sh
```

Сервер будет доступен на порту **8083**.

### 3. Использование веб-интерфейса

Откройте в браузере:
```
mcp-android-demo.html
```

Или запустите через веб-сервер:
```bash
# Запуск основного сервера
node server.js

# Откройте в браузере
http://localhost:3000/mcp-android-demo.html
```

## 📚 Доступные инструменты (Tools)

### 1. `list_emulators`

Получение списка всех доступных Android эмуляторов (AVD).

**Параметры:** нет

**Пример запроса:**
```json
{
  "name": "list_emulators",
  "arguments": {}
}
```

**Пример ответа:**
```json
{
  "success": true,
  "emulators": ["Pixel_5_API_31", "Pixel_7_API_33"],
  "count": 2,
  "message": "Найдено 2 эмулятор(ов)"
}
```

---

### 2. `start_emulator`

Запуск Android эмулятора по имени.

**Параметры:**
- `name` (string, обязательный) — имя эмулятора (AVD name)
- `options` (string, опционально) — дополнительные опции запуска
- `waitForBoot` (boolean, опционально) — ждать полной загрузки (по умолчанию: true)

**Пример запроса:**
```json
{
  "name": "start_emulator",
  "arguments": {
    "name": "Pixel_5_API_31",
    "options": "-no-snapshot-load",
    "waitForBoot": true
  }
}
```

**Пример ответа:**
```json
{
  "success": true,
  "message": "Эмулятор \"Pixel_5_API_31\" успешно запущен и готов к работе",
  "emulatorName": "Pixel_5_API_31",
  "bootTime": "45 секунд"
}
```

**Полезные опции запуска:**
- `-no-snapshot-load` — не загружать снимок состояния
- `-wipe-data` — очистить данные эмулятора
- `-no-audio` — без звука
- `-gpu swiftshader_indirect` — программный рендеринг

---

### 3. `stop_emulator`

Остановка запущенного эмулятора.

**Параметры:**
- `device` (string, опционально) — ID устройства (например: emulator-5554)

**Пример запроса:**
```json
{
  "name": "stop_emulator",
  "arguments": {
    "device": "emulator-5554"
  }
}
```

**Пример ответа:**
```json
{
  "success": true,
  "message": "Эмулятор emulator-5554 успешно остановлен",
  "device": "emulator-5554"
}
```

---

### 4. `get_emulator_status`

Получение статуса всех запущенных устройств и эмуляторов.

**Параметры:** нет

**Пример запроса:**
```json
{
  "name": "get_emulator_status",
  "arguments": {}
}
```

**Пример ответа:**
```json
{
  "success": true,
  "devices": [
    {
      "id": "emulator-5554",
      "status": "device",
      "model": "sdk_gphone64_x86_64",
      "device": "emulator64_x86_64",
      "type": "emulator"
    }
  ],
  "emulators": [...],
  "runningCount": 1,
  "totalDevices": 1,
  "message": "Запущено 1 эмулятор(ов)"
}
```

---

### 5. `install_apk`

Установка APK файла на эмулятор.

**Параметры:**
- `apkPath` (string, обязательный) — путь к APK файлу
- `device` (string, опционально) — ID устройства

**Пример запроса:**
```json
{
  "name": "install_apk",
  "arguments": {
    "apkPath": "/path/to/app.apk",
    "device": "emulator-5554"
  }
}
```

**Пример ответа:**
```json
{
  "success": true,
  "message": "APK успешно установлен: /path/to/app.apk",
  "apkPath": "/path/to/app.apk",
  "device": "emulator-5554",
  "output": "Success"
}
```

---

### 6. `execute_adb_command`

Выполнение произвольной ADB команды.

**Параметры:**
- `command` (string, обязательный) — команда без префикса "adb"
- `device` (string, опционально) — ID устройства

**Пример запроса:**
```json
{
  "name": "execute_adb_command",
  "arguments": {
    "command": "shell pm list packages",
    "device": "emulator-5554"
  }
}
```

**Пример ответа:**
```json
{
  "success": true,
  "command": "adb -s emulator-5554 shell pm list packages",
  "stdout": "package:com.android.chrome\npackage:com.google.android.gms\n...",
  "output": "..."
}
```

**Полезные команды:**
- `devices` — список устройств
- `shell pm list packages` — список установленных приложений
- `shell getprop` — свойства устройства
- `shell dumpsys battery` — информация о батарее
- `shell input text "Hello"` — ввод текста
- `shell input keyevent KEYCODE_HOME` — нажатие клавиши Home

---

### 7. `take_screenshot`

Создание снимка экрана с эмулятора.

**Параметры:**
- `outputPath` (string, опционально) — путь для сохранения (по умолчанию: ./screenshot.png)
- `device` (string, опционально) — ID устройства

**Пример запроса:**
```json
{
  "name": "take_screenshot",
  "arguments": {
    "outputPath": "./my_screenshot.png",
    "device": "emulator-5554"
  }
}
```

**Пример ответа:**
```json
{
  "success": true,
  "message": "Скриншот успешно сохранен: ./my_screenshot.png",
  "outputPath": "./my_screenshot.png",
  "device": "emulator-5554"
}
```

## 💡 Примеры использования

### Пример 1: Запуск эмулятора из агента

```javascript
const MCPAgent = require('./mcp-multi-agent');

const agent = new MCPAgent();

// Получаем список эмуляторов
const emulators = await agent.executeTool('android', 'list_emulators', {});
console.log('Доступные эмуляторы:', emulators.emulators);

// Запускаем первый эмулятор
if (emulators.emulators.length > 0) {
    await agent.executeTool('android', 'start_emulator', {
        name: emulators.emulators[0],
        waitForBoot: true
    });
}
```

### Пример 2: Автоматизация тестирования

```javascript
// 1. Запуск эмулятора
await agent.executeTool('android', 'start_emulator', {
    name: 'Test_Device_API_31',
    options: '-wipe-data',
    waitForBoot: true
});

// 2. Установка APK
await agent.executeTool('android', 'install_apk', {
    apkPath: './app/build/outputs/apk/debug/app-debug.apk'
});

// 3. Запуск приложения
await agent.executeTool('android', 'execute_adb_command', {
    command: 'shell am start -n com.example.app/.MainActivity'
});

// 4. Сделать скриншот
await agent.executeTool('android', 'take_screenshot', {
    outputPath: './test_results/screenshot_1.png'
});

// 5. Остановка эмулятора
await agent.executeTool('android', 'stop_emulator', {});
```

### Пример 3: Работа через HTTP API

```bash
# Получить список эмуляторов
curl -X POST http://localhost:8083/tools/execute \
  -H "Content-Type: application/json" \
  -d '{
    "name": "list_emulators",
    "arguments": {}
  }'

# Запустить эмулятор
curl -X POST http://localhost:8083/tools/execute \
  -H "Content-Type: application/json" \
  -d '{
    "name": "start_emulator",
    "arguments": {
      "name": "Pixel_5_API_31",
      "waitForBoot": true
    }
  }'
```

## 🔧 Настройка

### Переменные окружения

```bash
# Порт сервера (по умолчанию: 8083)
export MCP_ANDROID_PORT=8083

# Путь к Android SDK
export ANDROID_HOME=/path/to/android/sdk
export ANDROID_SDK_ROOT=/path/to/android/sdk
```

### Добавление в PATH

Для macOS/Linux:
```bash
# Добавьте в ~/.bashrc или ~/.zshrc
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$ANDROID_HOME/emulator
```

Для Windows:
```bash
# Добавьте в переменные окружения
ANDROID_HOME=C:\Users\YourName\AppData\Local\Android\Sdk
PATH=%PATH%;%ANDROID_HOME%\platform-tools;%ANDROID_HOME%\emulator
```

## 🐛 Устранение неполадок

### ADB не найден

**Проблема:** `ADB не найден. Убедитесь, что Android SDK установлен`

**Решение:**
1. Установите Android SDK через Android Studio
2. Добавьте путь к platform-tools в PATH
3. Проверьте: `adb version`

### Эмуляторы не найдены

**Проблема:** `Эмуляторы не найдены. Создайте AVD через Android Studio`

**Решение:**
1. Откройте Android Studio
2. Tools → Device Manager
3. Создайте новый виртуальный device (AVD)
4. Перезапустите сервер

### Эмулятор не запускается

**Проблема:** Эмулятор запускается долго или не запускается

**Решение:**
1. Проверьте, что у вас достаточно памяти
2. Используйте опцию `-no-snapshot-load`
3. Проверьте логи: `tail -f logs/android.log`
4. Попробуйте запустить эмулятор вручную: `emulator -avd YOUR_AVD_NAME`

### Порт уже занят

**Проблема:** `Error: listen EADDRINUSE: address already in use :::8083`

**Решение:**
```bash
# Найти процесс на порту 8083
lsof -ti:8083

# Остановить процесс
kill -9 $(lsof -ti:8083)

# Или измените порт
export MCP_ANDROID_PORT=8084
node mcp-server-android.js
```

## 📊 Мониторинг

### Health Check

```bash
curl http://localhost:8083/health
```

Ответ:
```json
{
  "status": "ok",
  "server": "Android Emulator Controller MCP",
  "version": "1.0.0",
  "port": 8083
}
```

### Список доступных tools

```bash
curl http://localhost:8083/tools
```

### Логи

Логи сервера сохраняются в `logs/android.log`:

```bash
# Просмотр логов в реальном времени
tail -f logs/android.log

# Последние 100 строк
tail -n 100 logs/android.log
```

## 🔐 Безопасность

⚠️ **Важно:**
- Этот сервер предназначен для локальной разработки
- Не разворачивайте его на публичных серверах без дополнительной защиты
- ADB команды выполняются с правами текущего пользователя
- Будьте осторожны с командой `execute_adb_command`

## 📝 Интеграция с агентом

Для интеграции с мульти-агентом, добавьте сервер в конфигурацию:

```javascript
// mcp-multi-agent.js
this.servers = {
    weather: { url: 'http://localhost:8080', name: 'Weather' },
    filesaver: { url: 'http://localhost:8081', name: 'FileSaver' },
    formatter: { url: 'http://localhost:8082', name: 'Formatter' },
    android: { url: 'http://localhost:8083', name: 'Android' }  // ← Добавить
};
```

Использование в агенте:

```javascript
// Запуск эмулятора через агента
await agent.executeTool('android', 'start_emulator', {
    name: 'Pixel_5_API_31'
});
```

## 🎯 Roadmap

- [ ] Поддержка множественных эмуляторов одновременно
- [ ] WebSocket для потоковой передачи логов
- [ ] Интеграция с Appium для автоматизации UI
- [ ] Запись видео с экрана эмулятора
- [ ] Управление сетевыми настройками
- [ ] Симуляция GPS координат

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи: `tail -f logs/android.log`
2. Убедитесь, что ADB работает: `adb devices`
3. Проверьте, что эмуляторы созданы: `emulator -list-avds`
4. Проверьте health check: `curl http://localhost:8083/health`

---

**Версия:** 1.0.0  
**Порт по умолчанию:** 8083  
**Лицензия:** MIT


