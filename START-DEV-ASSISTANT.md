# 🚀 Dev Assistant - Быстрый старт

> Запустите ассистента разработчика за 3 минуты!

---

## ⚡ Быстрый старт

### 1️⃣ Индексация проекта

```bash
node index-project-docs.js
```

**Что происходит:**
- 📄 Сканируются все `.md`, `.txt`, `.js`, `.ts`, `.py` файлы
- 🧠 Генерируются эмбеддинги через OpenAI API
- 💾 Создается `project-index.json` с векторной базой

**Время:** ~1-2 минуты (зависит от размера проекта)  
**Стоимость:** ~$0.01-0.05 (для среднего проекта)

### 2️⃣ Запуск сервера

```bash
npm start
```

### 3️⃣ Открыть веб-интерфейс

```
http://localhost:3000/dev-assistant
```

---

## 💬 Примеры вопросов

### 📚 Документация

```
"Как запустить проект?"
"Что такое MCP Chain Agent?"
"Покажи пример использования Document Indexer"
```

### 🔍 Код

```
"Где определена функция processDocument?"
"Покажи класс DevAssistant"
"Как работает функция chunkText?"
```

### 🔀 Git

```
"Какая текущая ветка?"
"Что изменилось?"
"Покажи последние 5 коммитов"
"Кто контрибьютор проекта?"
```

### 💡 Best Practices

```
"Best practices для MCP серверов"
"Как правильно создавать эмбеддинги?"
"Какие есть правила стиля кода?"
```

---

## 🎯 Специальные команды

| Команда | Описание |
|---------|----------|
| `/help` | Показать справку |
| `/status` | Статус проекта и git |
| `/docs <запрос>` | Искать только в документации |
| `/code <запрос>` | Искать только в коде |
| `/git <команда>` | Выполнить git команду |

---

## 🛠️ API примеры

### Отправить вопрос

```bash
curl -X POST http://localhost:3000/api/dev-assistant/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "Как запустить проект?"}'
```

### Получить статус

```bash
curl http://localhost:3000/api/dev-assistant/status
```

### Выполнить git команду

```bash
curl -X POST http://localhost:3000/api/dev-assistant/git/git_status \
  -H "Content-Type: application/json"
```

### Переиндексировать проект

```bash
curl -X POST http://localhost:3000/api/dev-assistant/index \
  -H "Content-Type: application/json"
```

---

## 📦 Что под капотом?

```
┌─────────────────────────────────────────┐
│   Development Assistant Architecture    │
└─────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐
│   RAG System │     │  Git Server  │
│  (Doc Index) │     │   (MCP)      │
└───────┬──────┘     └──────┬───────┘
        │                   │
        └──────┬────────────┘
               │
        ┌──────▼──────┐
        │   OpenAI    │
        │   GPT API   │
        └─────────────┘
```

### Компоненты

1. **Document Indexer** - индексация с эмбеддингами
2. **MCP Git Server** - доступ к git через MCP
3. **Dev Assistant** - оркестратор + AI
4. **Web UI** - красивый интерфейс

---

## 🎨 Возможности

### ✅ Что умеет

- [x] Поиск в документации по смыслу (не по ключевым словам!)
- [x] Находит функции, классы, методы в коде
- [x] Показывает git статус, ветки, коммиты
- [x] Понимает контекст проекта
- [x] Подсказывает best practices
- [x] Работает с любым git репозиторием
- [x] Индексирует JS, TS, Python, Go, Rust, Java
- [x] Красивый веб-интерфейс

### 🔜 В планах

- [ ] VS Code extension
- [ ] Поддержка больше языков (C++, Kotlin, Swift)
- [ ] Интеграция с GitHub API
- [ ] Кэширование запросов
- [ ] Incremental indexing

---

## 💰 Стоимость

### Индексация (один раз)

| Размер проекта | Токены | Стоимость |
|----------------|--------|-----------|
| Маленький (10 файлов) | ~7,000 | ~$0.001 |
| Средний (50 файлов) | ~70,000 | ~$0.01 |
| Большой (200 файлов) | ~280,000 | ~$0.04 |

### Запросы (каждый)

| Тип запроса | Токены | Стоимость |
|-------------|--------|-----------|
| Простой | ~1,500 | ~$0.0003 |
| Средний | ~3,000 | ~$0.0006 |
| Сложный | ~5,000 | ~$0.001 |

**Итого:** ~$0.05-0.10 за полную индексацию + 100 запросов

---

## 🐛 Решение проблем

### Ошибка: "Индекс не найден"

```bash
# Создайте индекс
node index-project-docs.js
```

### Ошибка: "Git not found"

```bash
# Проверьте git
git --version

# Инициализируйте репозиторий
git init
```

### Ошибка: "OpenAI API key not set"

```bash
# Создайте .env файл
echo "OPENAI_API_KEY=sk-..." > .env
```

### Медленные ответы

```javascript
// Уменьшите topK в dev-assistant.js
await this.searchDocs(query, 3); // было 5
```

---

## 📚 Полная документация

➡️ [DEV-ASSISTANT-GUIDE.md](./DEV-ASSISTANT-GUIDE.md)

**Разделы:**
- Архитектура
- API Endpoints
- Конфигурация
- Расширение функционала
- Best Practices

---

## 🎓 Примеры использования

### JavaScript

```javascript
const DevAssistant = require('./dev-assistant');

const assistant = new DevAssistant({
  projectPath: __dirname,
  apiKey: process.env.OPENAI_API_KEY
});

await assistant.loadIndex();

const result = await assistant.processMessage('Как работает RAG?');
console.log(result.answer);
```

### Python (через API)

```python
import requests

response = requests.post('http://localhost:3000/api/dev-assistant/chat', 
    json={'message': 'Покажи git статус'})

data = response.json()
print(data['answer'])
```

### Bash

```bash
# Функция для быстрого вопроса
ask() {
  curl -s -X POST http://localhost:3000/api/dev-assistant/chat \
    -H "Content-Type: application/json" \
    -d "{\"message\": \"$1\"}" | jq -r '.answer'
}

# Использование
ask "Что изменилось в проекте?"
```

---

## 🎯 Use Cases

### 1. Онбординг новых разработчиков

```
"Расскажи про архитектуру проекта"
"Какие основные компоненты?"
"Где находится точка входа?"
```

### 2. Code Review

```
"Покажи последние изменения"
"Кто изменял файл server.js?"
"Что изменилось в ветке feature/new-api?"
```

### 3. Документирование

```
"Покажи все экспортируемые функции"
"Какие есть MCP серверы?"
"Опиши структуру проекта"
```

### 4. Отладка

```
"Где используется функция fetchData?"
"Покажи все обработчики ошибок"
"Где определен класс DocumentIndexer?"
```

---

## ⚙️ Настройка

### Добавить больше расширений файлов

```javascript
// В index-project-docs.js
this.codeExtensions.push('.cpp', '.h', '.swift');
```

### Изменить размер чанков

```javascript
// В document-indexer.js
this.chunkSize = 1000; // было 500
this.chunkOverlap = 100; // было 50
```

### Кастомизировать system prompt

```javascript
// В dev-assistant.js
this.systemPrompt = `Вы — эксперт по ${projectName}...`;
```

---

## 🚀 Продакшн деплой

### Docker

```dockerfile
FROM node:18
WORKDIR /app
COPY . .
RUN npm install
RUN node index-project-docs.js
EXPOSE 3000
CMD ["npm", "start"]
```

### Docker Compose

```yaml
version: '3'
services:
  dev-assistant:
    build: .
    ports:
      - "3000:3000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./project-index.json:/app/project-index.json
```

### systemd service

```ini
[Unit]
Description=Dev Assistant
After=network.target

[Service]
Type=simple
User=nodejs
WorkingDirectory=/opt/dev-assistant
ExecStart=/usr/bin/node server.js
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

---

## 📈 Мониторинг

### Логирование

```javascript
// В dev-assistant.js
console.log('[DevAssistant] Query:', message);
console.log('[DevAssistant] Intent:', intent);
console.log('[DevAssistant] Docs found:', context.docs.length);
console.log('[DevAssistant] Tokens used:', result.usage.total_tokens);
```

### Метрики

```javascript
const metrics = {
  totalQueries: 0,
  totalTokens: 0,
  avgResponseTime: 0
};

// Track в каждом запросе
metrics.totalQueries++;
metrics.totalTokens += result.usage.total_tokens;
```

---

**Готово! 🎉 Теперь у вас есть мощный ассистент для разработки!**

Вопросы? → [DEV-ASSISTANT-GUIDE.md](./DEV-ASSISTANT-GUIDE.md)
