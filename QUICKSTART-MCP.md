# 🚀 Быстрый старт MCP Chain Agent

## Что было добавлено

В проект добавлена система MCP Chain Agent - агент-оркестратор для координации цепочек MCP инструментов.

### Новые файлы

```
GorAgent/
├── mcp-servers.js              # ✨ Реализация MCP серверов
├── mcp-agent.js                # ✨ Агент-оркестратор
├── mcp-chain-demo.html         # ✨ Демо-интерфейс
├── MCP-CHAIN-GUIDE.md          # ✨ Полное руководство
├── QUICKSTART-MCP.md           # ✨ Этот файл
├── mcp-output/                 # ✨ Директория для выходных файлов
│   ├── example-mcp-basics.md
│   ├── example-mcp-nodejs.md
│   └── example-mcp-security.md
└── server.js                   # 🔄 Обновлён (новые API эндпоинты)
```

## 3 способа использования

### 1️⃣ Через веб-интерфейс (самый простой)

1. Запустите сервер:
```bash
npm start
```

2. Откройте в браузере:
```
http://localhost:3000/mcp-demo
```

3. Введите запрос и нажмите "▶️ Запустить цепочку"

### 2️⃣ Через API

```bash
curl -X POST http://localhost:3000/api/mcp/chain \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Node.js best practices",
    "options": {
      "searchLimit": 5,
      "summaryFormat": "bullet-points",
      "saveFormat": "md"
    }
  }'
```

### 3️⃣ Через Node.js код

```javascript
const MCPAgent = require('./mcp-agent');

const agent = new MCPAgent();

await agent.executeChain('React Hooks best practices', {
  searchLimit: 5,
  summaryFormat: 'bullet-points'
});
```

## Как работает цепочка

```
Ваш запрос
    ↓
┌────────────────┐
│  searchDocs    │  Поиск документации
└────────────────┘
    ↓
┌────────────────┐
│  summarize     │  Создание резюме
└────────────────┘
    ↓
┌────────────────┐
│  saveToFile    │  Сохранение файла
└────────────────┘
    ↓
Готовый файл в mcp-output/
```

## Примеры запросов

- `MCP архитектура и best practices`
- `Node.js безопасность и валидация`
- `React Hooks оптимизация`
- `GraphQL vs REST API`
- `Docker контейнеризация`

## Тестовые примеры

Запустите готовые примеры:

```bash
node mcp-agent.js
```

Это создаст 3 файла в `mcp-output/`:
- example-mcp-basics.md
- example-mcp-security.md
- example-mcp-nodejs.md

## API эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/mcp/chain` | Запустить цепочку |
| POST | `/api/mcp/tool` | Выполнить отдельный инструмент |
| GET | `/api/mcp/stats` | Получить статистику |
| GET | `/api/mcp/files` | Список файлов |
| GET | `/api/mcp/files/:filename` | Прочитать файл |

## Доступные инструменты

### 1. searchDocs
Поиск в документации по запросу

### 2. summarize
Создание краткого резюме из текста

### 3. saveToFile
Сохранение результатов в файл (MD/TXT/JSON)

## Настройка цепочки

```javascript
{
  query: "Ваш запрос",
  options: {
    searchLimit: 5,              // Количество результатов поиска (1-10)
    summaryFormat: "bullet-points", // bullet-points | paragraph | structured
    summaryMaxLength: 200,       // Длина резюме в словах (50-500)
    saveFormat: "md",            // md | txt | json
    saveFilename: "custom.md"    // Опционально: кастомное имя файла
  }
}
```

## Статистика

После запуска примеров:

```
✅ Всего выполнений: 3
✅ Успешных: 100%
⏱️ Среднее время: ~800ms
📁 Файлов сохранено: 3
```

## Дополнительная документация

- [MCP-CHAIN-GUIDE.md](./MCP-CHAIN-GUIDE.md) - Полное руководство
- [README.md](./README.md) - Основная документация проекта

## Что дальше?

1. **Попробуйте веб-интерфейс** - http://localhost:3000/mcp-demo
2. **Запустите примеры** - `node mcp-agent.js`
3. **Создайте свои запросы** - используйте API или код
4. **Посмотрите результаты** - откройте файлы в `mcp-output/`

---

**🎉 Готово! Агент настроен и работает!**



