# 🔗 MCP Multi-Server Agent - Руководство

## Что это такое?

**MCP Multi-Server Agent** — это агент-оркестратор для координации работы **нескольких независимых MCP серверов**. Каждый MCP сервер — это отдельный процесс со своими инструментами.

### Отличие от одиночного MCP сервера

❌ **Неправильно (один сервер с несколькими tools):**
```
MCP Server
├── tool1
├── tool2
└── tool3
```

✅ **Правильно (несколько MCP серверов):**
```
Weather MCP Server (порт 8080)
├── get_weather

Formatter MCP Server (порт 8082)
├── format_weather_report
├── create_table
├── beautify_json
└── generate_summary

FileSaver MCP Server (порт 8081)
├── save_to_file
├── append_to_file
├── list_files
└── read_file
```

## Архитектура

```
┌─────────────────────────────────────────────┐
│           USER REQUEST                      │
│         "Погода в Москве"                   │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│        MCP MULTI-AGENT (оркестратор)        │
│            (координирует цепочку)           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ ШАГ 1: Weather MCP (localhost:8080)         │
│ • Получение данных о погоде                 │
│ • Tool: get_weather(city: "Москва")         │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ ШАГ 2: Formatter MCP (localhost:8082)       │
│ • Форматирование в читаемый вид             │
│ • Tool: format_weather_report()             │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│ ШАГ 3: FileSaver MCP (localhost:8081)       │
│ • Сохранение в файл                         │
│ • Tool: save_to_file()                      │
└─────────────────────────────────────────────┘
                    ↓
            Готовый файл!
```

## Быстрый старт

### 1. Запуск MCP серверов

#### Вариант A: Автоматический запуск

```bash
chmod +x start-mcp-servers.sh
./start-mcp-servers.sh
```

#### Вариант B: Ручной запуск

**Терминал 1 - Formatter MCP:**
```bash
node mcp-server-formatter.js
```

**Терминал 2 - FileSaver MCP:**
```bash
node mcp-server-filesaver.js
```

**Терминал 3 - Weather MCP:**
```bash
# Ваш существующий сервер погоды (порт 8080)
```

**Терминал 4 - Главный сервер:**
```bash
npm start
```

### 2. Откройте веб-интерфейс

```
http://localhost:3000/mcp-multi-demo
```

### 3. Запустите цепочку

1. Проверьте, что все серверы доступны (зелёные карточки)
2. Введите название города
3. Выберите формат и язык
4. Нажмите "▶️ Запустить цепочку"

## MCP Серверы

### 1. Weather MCP (порт 8080)

**Ваш существующий сервер погоды**

**Tools:**
- `get_weather` - получение погоды по городу

### 2. Formatter MCP (порт 8082)

**Новый сервер для форматирования данных**

**Tools:**
- `format_weather_report` - форматирование погоды (MD/HTML/Text/JSON)
- `create_table` - создание таблиц из данных
- `beautify_json` - красивое форматирование JSON
- `generate_summary` - генерация резюме

**Файл:** `mcp-server-formatter.js`

### 3. FileSaver MCP (порт 8081)

**Новый сервер для работы с файлами**

**Tools:**
- `save_to_file` - сохранение в файл
- `append_to_file` - добавление к файлу
- `list_files` - список файлов
- `read_file` - чтение файла

**Файл:** `mcp-server-filesaver.js`

**Директория:** `mcp-data/`

## Использование

### Через веб-интерфейс

```
http://localhost:3000/mcp-multi-demo
```

### Через API

```bash
curl -X POST http://localhost:3000/api/mcp-multi/weather-chain \
  -H "Content-Type: application/json" \
  -d '{
    "city": "Москва",
    "options": {
      "format": "markdown",
      "language": "ru",
      "includeEmoji": true,
      "saveFormat": "md"
    }
  }'
```

### Через Node.js

```javascript
const MCPMultiAgent = require('./mcp-multi-agent');

const agent = new MCPMultiAgent();

// Запуск цепочки
await agent.executeWeatherChain('Москва', {
  format: 'markdown',
  language: 'ru'
});

// Проверка серверов
const status = await agent.checkAllServers();
console.log(status);

// Статистика
const stats = agent.getStats();
console.log(stats);
```

## API Эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/mcp-multi/weather-chain` | Запуск цепочки погоды |
| GET | `/api/mcp-multi/check-servers` | Проверка серверов |
| GET | `/api/mcp-multi/all-tools` | Все tools от всех серверов |
| GET | `/api/mcp-multi/stats` | Статистика |
| GET | `/api/mcp-multi/history` | История выполнений |

## Примеры

### Пример 1: Погода в Markdown

**Запрос:**
```json
{
  "city": "Москва",
  "options": {
    "format": "markdown",
    "language": "ru"
  }
}
```

**Результат:** `mcp-data/weather-moskva-2025-12-19.md`

```markdown
# 🌤️ Прогноз погоды

**Местоположение:** Москва

---

🌡️ **Температура:** 15°C
🤔 **Ощущается как:** 13°C
💧 **Влажность:** 60%
💨 **Ветер:** 5 м/с
🔽 **Давление:** 1013 гПа

**Описание:** Облачно с прояснениями

---

*Время обновления: 19.12.2025, 13:00:00*
```

### Пример 2: Погода в HTML

**Запрос:**
```json
{
  "city": "Шерегеш",
  "options": {
    "format": "html",
    "language": "ru"
  }
}
```

**Результат:** `mcp-data/weather-sheregesh-2025-12-19.html`

HTML файл с красивым оформлением.

### Пример 3: Несколько городов

```bash
# Москва
node -e "
const agent = require('./mcp-multi-agent');
new agent().executeWeatherChain('Москва');
"

# Шерегеш
node -e "
const agent = require('./mcp-multi-agent');
new agent().executeWeatherChain('Шерегеш');
"

# Братск
node -e "
const agent = require('./mcp-multi-agent');
new agent().executeWeatherChain('Братск');
"
```

## Запуск примеров

```bash
node mcp-multi-agent.js
```

Это запустит 3 примера:
1. Москва (Markdown)
2. Шерегеш (HTML)
3. Братск (Text)

## Проверка работы

### Проверка MCP серверов

```bash
# Formatter MCP
curl http://localhost:8082/health
curl http://localhost:8082/tools

# FileSaver MCP
curl http://localhost:8081/health
curl http://localhost:8081/tools

# Weather MCP
curl http://localhost:8080/health
curl http://localhost:8080/tools
```

### Просмотр файлов

```bash
ls -lh mcp-data/
cat mcp-data/weather-moskva-*.md
```

## Отладка

### Проблема: Сервер не запускается

**Решение:** Проверьте, не занят ли порт:

```bash
lsof -i :8081
lsof -i :8082
```

### Проблема: "Server unavailable"

**Решение:** Убедитесь, что все серверы запущены:

```bash
curl http://localhost:8080/health
curl http://localhost:8081/health
curl http://localhost:8082/health
```

### Проблема: Файлы не сохраняются

**Решение:** Проверьте директорию:

```bash
mkdir -p mcp-data
chmod 755 mcp-data
```

## Расширение

### Добавление нового MCP сервера

1. **Создайте файл `mcp-server-custom.js`:**

```javascript
const express = require('express');
const app = express();
const PORT = 8083;

app.use(express.json());

app.get('/tools', (req, res) => {
    res.json([{
        name: 'my_custom_tool',
        description: 'Описание',
        inputSchema: { /* ... */ }
    }]);
});

app.post('/tools/execute', async (req, res) => {
    // Ваша логика
});

app.listen(PORT);
```

2. **Обновите агента:**

```javascript
const agent = new MCPMultiAgent({
    weatherUrl: 'http://localhost:8080',
    formatterUrl: 'http://localhost:8082',
    fileSaverUrl: 'http://localhost:8081',
    customUrl: 'http://localhost:8083' // Новый
});
```

3. **Создайте новую цепочку:**

```javascript
async executeCustomChain() {
    const result1 = await this.callMCPServer(
        this.servers.weather,
        'get_weather',
        { city: 'Москва' }
    );
    
    const result2 = await this.callMCPServer(
        this.servers.custom,
        'my_custom_tool',
        { data: result1 }
    );
    
    return result2;
}
```

## Сравнение подходов

### Одиночный MCP сервер (старый подход)

**Плюсы:**
- Проще в разработке
- Меньше процессов

**Минусы:**
- Всё в одном файле
- Сложно масштабировать
- Нет изоляции

### Множественные MCP серверы (новый подход)

**Плюсы:**
- ✅ Каждый сервер независим
- ✅ Легко добавлять новые
- ✅ Изоляция ответственности
- ✅ Можно использовать сервера от разных разработчиков
- ✅ Горизонтальное масштабирование

**Минусы:**
- Больше процессов
- Нужна координация

## Статистика

После запуска примеров:

```
✅ Всего выполнений: 3
✅ Успешных: 100%
⏱️ Среднее время: ~1200ms
📁 Файлов создано: 3
```

## Дополнительные материалы

- [README.md](./README.md) - Общая документация
- [QUICKSTART-MCP.md](./QUICKSTART-MCP.md) - Быстрый старт (старая версия)
- [mcp-server-formatter.js](./mcp-server-formatter.js) - Код Formatter MCP
- [mcp-server-filesaver.js](./mcp-server-filesaver.js) - Код FileSaver MCP
- [mcp-multi-agent.js](./mcp-multi-agent.js) - Код агента-оркестратора

---

**Готово! Используйте MCP Multi-Server Agent! 🎉**




