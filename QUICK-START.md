# 🚀 Быстрый старт - Android MCP с вашим агентом

## ✅ Готово к использованию!

Я интегрировал Android MCP сервер в ваш веб-агент на http://localhost:3000

## 3 шага для запуска

### 1️⃣ Запустите MCP серверы

```bash
bash start-mcp-servers.sh
```

Это запустит:
- Weather MCP (порт 8080)
- Formatter MCP (порт 8082)
- FileSaver MCP (порт 8081)
- **Android MCP (порт 8083)** ← Новый!

### 2️⃣ Запустите ваш агент

```bash
node server.js
```

Агент запустится на http://localhost:3000

### 3️⃣ Используйте в чате

Откройте http://localhost:3000 и напишите:

```
"Покажи список эмуляторов"
```

Агент автоматически:
1. Увидит доступные Android инструменты
2. Вызовет нужный инструмент
3. Покажет результат

## 💬 Примеры команд

Просто напишите агенту:

- **"Покажи список эмуляторов"** → список всех AVD
- **"Открой эмулятор Pixel_5_API_31"** → запустит эмулятор
- **"Какие эмуляторы сейчас работают?"** → проверит статус
- **"Останови эмулятор"** → остановит
- **"Установи APK /path/to/app.apk"** → установит приложение
- **"Сделай скриншот"** → создаст screenshot.png
- **"Выполни команду adb shell pm list packages"** → выполнит ADB команду

## 🔍 Проверка работы

### Через API:

```bash
# Статус всех MCP серверов
curl http://localhost:3000/api/health | jq '.mcp'

# Список всех инструментов
curl http://localhost:3000/api/mcp/tools | jq '.toolsByServer.android'
```

### Через веб-интерфейс:

1. Откройте http://localhost:3000
2. Нажмите ⚙️ (настройки)
3. Прокрутите вниз до раздела "MCP Tools"
4. Увидите все Android инструменты

## 📦 Что добавлено

### Новые файлы:
- `mcp-server-android.js` - MCP сервер для Android
- `test-android-agent.js` - тесты
- `mcp-android-demo.html` - веб-интерфейс
- Документация (несколько .md файлов)

### Изменения в server.js:
- ✅ Поддержка множественных MCP серверов
- ✅ Автоматическое определение сервера для инструмента
- ✅ Обновленные API эндпоинты
- ✅ Health check для всех серверов

### Android инструменты:
1. `android__list_emulators` - список эмуляторов
2. `android__start_emulator` - запуск
3. `android__stop_emulator` - остановка
4. `android__get_emulator_status` - статус
5. `android__install_apk` - установка APK
6. `android__execute_adb_command` - ADB команды
7. `android__take_screenshot` - скриншоты

## ⚠️ Требования

### ADB должен быть доступен:

```bash
# Проверка
adb version

# Если не найден, добавьте в PATH:
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$ANDROID_HOME/emulator

# Добавьте в ~/.bashrc или ~/.zshrc для постоянного использования
```

### Создайте эмулятор (если нет):

1. Откройте Android Studio
2. **Tools** → **Device Manager**
3. **Create Device**
4. Выберите Pixel 5, API 31
5. **Finish**

Проверка: `emulator -list-avds`

## 🧪 Тестирование

### Вариант 1: Через чат

```
Вы → Агент:
"Покажи список эмуляторов"

Агент:
"Проверяю доступные эмуляторы...
[Вызывает android__list_emulators]

Найдено 2 эмулятора:
1. Pixel_5_API_31
2. Pixel_7_API_33"
```

### Вариант 2: Через API

```bash
curl -X POST http://localhost:3000/api/mcp/execute \
  -H "Content-Type: application/json" \
  -d '{
    "toolName": "android__list_emulators",
    "arguments": {}
  }'
```

### Вариант 3: Тестовый скрипт

```bash
node test-android-agent.js
```

## 📊 Мониторинг

### Логи сервера:

```bash
# Агент
tail -f logs/server.log  # если настроен

# Android MCP
tail -f logs/android.log

# Все MCP серверы
tail -f logs/*.log
```

### Health check:

```bash
# Через curl
curl http://localhost:3000/api/health

# Через браузер
http://localhost:3000/api/health
```

## 🐛 Если что-то не работает

### 1. Проверьте, что все серверы запущены:

```bash
# Проверка портов
lsof -ti:3000  # Агент
lsof -ti:8080  # Weather MCP
lsof -ti:8081  # FileSaver MCP
lsof -ti:8082  # Formatter MCP
lsof -ti:8083  # Android MCP
```

### 2. Проверьте health:

```bash
curl http://localhost:3000/api/health | jq '.mcp.servers'
```

Все серверы должны быть `"status": "online"`

### 3. Проверьте инструменты:

```bash
curl http://localhost:3000/api/mcp/tools | jq '.totalTools'
```

Должно быть > 0

### 4. Проверьте логи:

```bash
# Последние ошибки
tail -n 50 logs/android.log | grep -i error
```

## 📚 Документация

- **[ANDROID-INTEGRATION-DONE.md](./ANDROID-INTEGRATION-DONE.md)** - Что было сделано
- **[START-ANDROID-MCP.md](./START-ANDROID-MCP.md)** - Быстрый старт Android MCP
- **[ANDROID-MCP-GUIDE.md](./ANDROID-MCP-GUIDE.md)** - Полное руководство (500+ строк)
- **[ANDROID-AGENT-INTEGRATION.md](./ANDROID-AGENT-INTEGRATION.md)** - Детальная интеграция

## 💡 Полезные команды

```bash
# Запустить все
bash start-mcp-servers.sh && node server.js

# Остановить все MCP серверы
pkill -f "mcp-server"

# Проверить запущенные процессы
ps aux | grep "mcp-server"

# Очистить логи
rm -f logs/*.log

# Перезапустить конкретный сервер
pkill -f "mcp-server-android" && node mcp-server-android.js &
```

---

## ✨ Готово!

Теперь просто:

1. **Запустите:** `bash start-mcp-servers.sh`
2. **Запустите:** `node server.js`
3. **Откройте:** http://localhost:3000
4. **Напишите:** "Покажи список эмуляторов"

И агент сделает всё сам! 🎉



