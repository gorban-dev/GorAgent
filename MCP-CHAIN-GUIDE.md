# 🔗 MCP Chain Agent - Руководство по использованию

## Быстрый старт

### 1. Запуск сервера

```bash
npm start
```

Сервер запустится на http://localhost:3000

### 2. Открытие демо-интерфейса

Откройте в браузере:
```
http://localhost:3000/mcp-demo
```

### 3. Первый запуск цепочки

1. В поле "Поисковый запрос" введите тему, например:
   - `MCP архитектура и лучшие практики`
   - `Node.js безопасность`
   - `GraphQL оптимизация`

2. Настройте параметры (или оставьте по умолчанию):
   - Количество результатов: 5
   - Формат резюме: Маркированный список
   - Максимальная длина: 200 слов
   - Формат файла: Markdown

3. Нажмите "▶️ Запустить цепочку"

4. Наблюдайте за выполнением:
   - ШАГ 1: Поиск документации
   - ШАГ 2: Создание резюме
   - ШАГ 3: Сохранение файла

5. Результат сохраняется в `mcp-output/`

## Как это работает

### Цепочка выполнения

```
Запрос пользователя
       ↓
┌──────────────────┐
│  searchDocs      │  ← Поиск по запросу
└──────────────────┘
       ↓
  Результаты поиска (список документов)
       ↓
┌──────────────────┐
│  summarize       │  ← Создание резюме
└──────────────────┘
       ↓
  Краткое резюме
       ↓
┌──────────────────┐
│  saveToFile      │  ← Сохранение
└──────────────────┘
       ↓
  Готовый файл
```

### Что происходит на каждом шаге

**ШАГ 1: searchDocs**
- Принимает поисковый запрос
- Ищет релевантные документы в базе знаний
- Ранжирует результаты по релевантности
- Возвращает топ N документов

**ШАГ 2: summarize**
- Получает найденные документы
- Анализирует содержимое
- Извлекает ключевые моменты
- Форматирует резюме (bullet-points/paragraph/structured)

**ШАГ 3: saveToFile**
- Формирует итоговый документ
- Добавляет метаданные (дата, запрос, статистика)
- Сохраняет в выбранном формате (MD/TXT/JSON)
- Возвращает путь к файлу

## Примеры использования

### Пример 1: Исследование технологии

**Запрос:** `Kubernetes масштабирование приложений`

**Настройки:**
- Результатов: 7
- Формат резюме: Structured
- Длина: 300 слов
- Формат файла: Markdown

**Результат:** Подробный обзор методов масштабирования в Kubernetes

### Пример 2: Изучение best practices

**Запрос:** `React Hooks лучшие практики`

**Настройки:**
- Результатов: 5
- Формат резюме: Bullet-points
- Длина: 200 слов
- Формат файла: Markdown

**Результат:** Список рекомендаций по использованию React Hooks

### Пример 3: Анализ безопасности

**Запрос:** `JWT токены безопасность уязвимости`

**Настройки:**
- Результатов: 8
- Формат резюме: Paragraph
- Длина: 250 слов
- Формат файла: Text

**Результат:** Параграф о безопасности JWT с примерами

## Использование через API

### Запуск полной цепочки

```bash
curl -X POST http://localhost:3000/api/mcp/chain \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Docker контейнеризация",
    "options": {
      "searchLimit": 5,
      "summaryFormat": "bullet-points",
      "summaryMaxLength": 200,
      "saveFormat": "md"
    }
  }'
```

**Ответ:**
```json
{
  "success": true,
  "execution": {
    "id": "chain_1703001234567",
    "query": "Docker контейнеризация",
    "status": "completed",
    "totalDuration": 1523,
    "steps": [...]
  },
  "summary": "### Краткое резюме\n• Docker позволяет...",
  "savedFile": "/path/to/mcp-output/search-docker-2024-12-19.md",
  "searchResultsCount": 5
}
```

### Выполнение отдельного шага

**Только поиск:**
```bash
curl -X POST http://localhost:3000/api/mcp/tool \
  -H "Content-Type: application/json" \
  -d '{
    "toolName": "searchDocs",
    "args": {
      "query": "Python async/await",
      "limit": 3
    }
  }'
```

**Только суммаризация:**
```bash
curl -X POST http://localhost:3000/api/mcp/tool \
  -H "Content-Type: application/json" \
  -d '{
    "toolName": "summarize",
    "args": {
      "text": "Длинный текст документации...",
      "maxLength": 150,
      "format": "paragraph"
    }
  }'
```

**Только сохранение:**
```bash
curl -X POST http://localhost:3000/api/mcp/tool \
  -H "Content-Type: application/json" \
  -d '{
    "toolName": "saveToFile",
    "args": {
      "content": "Содержимое для сохранения",
      "filename": "my-notes.md",
      "format": "md"
    }
  }'
```

### Получение статистики

```bash
curl http://localhost:3000/api/mcp/stats
```

**Ответ:**
```json
{
  "totalExecutions": 15,
  "successfulExecutions": 14,
  "failedExecutions": 1,
  "successRate": "93.3%",
  "avgDuration": 1456
}
```

### Список сохранённых файлов

```bash
curl http://localhost:3000/api/mcp/files
```

**Ответ:**
```json
{
  "files": [
    {
      "filename": "search-docker-2024-12-19.md",
      "size": 4532,
      "created": "2024-12-19T10:30:00.000Z",
      "modified": "2024-12-19T10:30:00.000Z"
    }
  ],
  "count": 1
}
```

### Чтение файла

```bash
curl http://localhost:3000/api/mcp/files/search-docker-2024-12-19.md
```

## Использование из Node.js

### Базовое использование

```javascript
const MCPAgent = require('./mcp-agent');

const agent = new MCPAgent();

// Запуск цепочки
const result = await agent.executeChain('Machine Learning basics', {
  searchLimit: 5,
  summaryFormat: 'bullet-points',
  saveFormat: 'md'
});

console.log('Файл сохранён:', result.savedFile);
console.log('Резюме:', result.summary);
```

### Расширенное использование

```javascript
const MCPAgent = require('./mcp-agent');

const agent = new MCPAgent();

// Запуск нескольких цепочек
const queries = [
  'React Hooks',
  'Vue 3 Composition API',
  'Angular Signals'
];

for (const query of queries) {
  await agent.executeChain(query, {
    searchLimit: 5,
    summaryFormat: 'structured',
    summaryMaxLength: 250,
    saveFormat: 'md'
  });
}

// Получение статистики
const stats = agent.getStats();
console.log('Выполнено цепочек:', stats.totalExecutions);
console.log('Успешных:', stats.successfulExecutions);
console.log('Среднее время:', stats.avgDuration, 'ms');

// Получение всех файлов
const files = await agent.getSavedFiles();
console.log('Сохранено файлов:', files.length);
```

### Обработка ошибок

```javascript
const MCPAgent = require('./mcp-agent');

const agent = new MCPAgent();

try {
  const result = await agent.executeChain('Some query', {
    searchLimit: 5
  });
  
  if (result.success) {
    console.log('✅ Успех:', result.savedFile);
  } else {
    console.error('❌ Ошибка:', result.error);
  }
} catch (error) {
  console.error('❌ Критическая ошибка:', error.message);
}
```

## Запуск тестовых примеров

### Из командной строки

```bash
node mcp-agent.js
```

Это запустит 3 примера:
1. Базовый поиск о MCP
2. Поиск о безопасности с форматом параграфа
3. Поиск о интеграции со структурированным резюме

### Из Node.js кода

```javascript
const MCPAgent = require('./mcp-agent');

const agent = new MCPAgent();

// Запуск примеров
await agent.runExamples();

// Просмотр результатов
const stats = agent.getStats();
console.log(stats);
```

## Структура проекта

```
GorAgent/
├── mcp-servers.js          # Реализация MCP серверов
├── mcp-agent.js            # Агент-оркестратор
├── mcp-chain-demo.html     # Демо-интерфейс
├── server.js               # Express сервер (обновлён)
├── mcp-output/             # Директория для выходных файлов
│   ├── search-*.md
│   ├── example-*.md
│   └── ...
└── MCP-CHAIN-GUIDE.md      # Это руководство
```

## Расширение функциональности

### Добавление нового MCP сервера

1. Откройте `mcp-servers.js`
2. Добавьте описание инструмента в метод `getTools()`:

```javascript
{
  name: 'myNewTool',
  description: 'Описание инструмента',
  inputSchema: {
    type: 'object',
    properties: {
      param1: {
        type: 'string',
        description: 'Описание параметра'
      }
    },
    required: ['param1']
  }
}
```

3. Добавьте метод для выполнения:

```javascript
async myNewTool(args) {
  const { param1 } = args;
  
  // Ваша логика
  
  return {
    success: true,
    result: 'результат'
  };
}
```

4. Добавьте вызов в `executeTool()`:

```javascript
case 'myNewTool':
  return await this.myNewTool(args);
```

### Создание собственной цепочки

```javascript
const MCPAgent = require('./mcp-agent');

class MyCustomAgent extends MCPAgent {
  async executeCustomChain(query) {
    // Шаг 1: Поиск
    const searchResult = await this.executeTool('searchDocs', {
      query,
      limit: 10
    });
    
    // Шаг 2: Ваша кастомная логика
    const processed = this.processResults(searchResult);
    
    // Шаг 3: Суммаризация
    const summary = await this.executeTool('summarize', {
      text: processed,
      format: 'structured'
    });
    
    // Шаг 4: Сохранение
    const saved = await this.executeTool('saveToFile', {
      content: summary.summary,
      filename: `custom-${Date.now()}.md`,
      format: 'md'
    });
    
    return saved;
  }
  
  processResults(searchResult) {
    // Ваша логика обработки
    return searchResult.results.map(r => r.snippet).join('\n\n');
  }
}

const agent = new MyCustomAgent();
await agent.executeCustomChain('My custom query');
```

## Полезные советы

### 1. Выбор формата резюме

- **bullet-points** - для быстрого сканирования информации
- **paragraph** - для связного текста
- **structured** - для детального анализа с разделами

### 2. Оптимизация производительности

- Для быстрых поисков используйте `searchLimit: 3-5`
- Для глубокого анализа используйте `searchLimit: 7-10`
- Среднее время выполнения: ~1-2 секунды

### 3. Именование файлов

Файлы автоматически именуются по паттерну:
```
search-{query-slug}-{date}.{format}
```

Для кастомного имени передайте `saveFilename`:
```javascript
{
  saveFilename: 'my-custom-name.md'
}
```

### 4. Работа с большими объёмами

Для обработки списка запросов:
```javascript
const queries = ['query1', 'query2', 'query3'];

for (const query of queries) {
  await agent.executeChain(query);
  // Небольшая пауза между запросами
  await new Promise(r => setTimeout(r, 500));
}
```

## Troubleshooting

### Проблема: Файлы не сохраняются

**Решение:** Проверьте наличие директории `mcp-output/`. Она создаётся автоматически при первом запуске.

### Проблема: Долгое выполнение

**Решение:** Уменьшите `searchLimit` или `summaryMaxLength`.

### Проблема: Ошибка "Cannot find module './mcp-agent'"

**Решение:** Убедитесь, что вы запускаете код из корневой директории проекта.

### Проблема: API возвращает 500 ошибку

**Решение:** Проверьте логи сервера в консоли. Возможно, не хватает прав на запись файлов.

## Дополнительные ресурсы

- [README.md](./README.md) - Основная документация проекта
- [server.js](./server.js) - Код сервера с API эндпоинтами
- [mcp-servers.js](./mcp-servers.js) - Реализация MCP серверов
- [mcp-agent.js](./mcp-agent.js) - Логика агента-оркестратора

---

**Создано для GorAgent**

